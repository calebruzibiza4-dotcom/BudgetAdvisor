services:
  - type: web
    name: budget-advisor-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FRONTEND_ORIGIN
        value: https://budget-advisor-jgl9.vercel.app
      - key: NODE_ENV
        value: production
